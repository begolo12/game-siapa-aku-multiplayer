# PostgreSQL connection string. Required on Vercel because filesystem fallback
# storage is not persistent in serverless functions.
DATABASE_URL="postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
